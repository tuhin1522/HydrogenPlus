// Prisma Schema - Main Configuration File
// All models are organized into separate files in /schemas directory for better maintainability

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}